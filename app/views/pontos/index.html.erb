<h1>Listing pontos</h1>

<table>
  <tr>
    <th>Data</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @pontos.each do |ponto| %>
  <tr>
    <td><%= ponto.data %></td>
    <td><%= link_to 'Show', ponto %></td>
    <td><%= link_to 'Edit', edit_ponto_path(ponto) %></td>
    <td><%= link_to 'Destroy', ponto, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Ponto', new_ponto_path %>
